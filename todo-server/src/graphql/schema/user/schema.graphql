type User {
    username: String
    email: String
    password: String
}

input UserInput {
    username: String
    email: String
    password: String
}

type StatusCode {
    code: Int
}

type Query {
    loginUser(email:String, password:String): StatusCode
}

type Mutation {
    createUser(UserInput:UserInput): StatusCode
}